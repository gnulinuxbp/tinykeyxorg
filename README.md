# tinykeyxorg
English: Small FLTK 1.3 GUI for Tiny Core Linux to configure and persist the Xorg keyboard layout (model, layout, variant, options).  Español: Pequeña interfaz gráfica en FLTK 1.3 para Tiny Core Linux que configura y hace persistente el teclado de Xorg (modelo, layout, variante, opciones).
